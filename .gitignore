node_modules/
.env
uploads/
*.log
.DS_Store
# Backups and temp files
*.bak
backup_*.sql
patch_*.py

# Unused legacy fonts (kept in VM only)
backend/src/fonts/THSarabunNew*.ttf
*.bak*
*.current
*.bak~*
*.bak.before-*

# Throwaway migration scripts at repo root (one-time use, not tracked)
migration_*.sql
# BUT track migrations in db/migrations/ (committed for reproducibility)
!db/migrations/
!db/migrations/*.sql

*.bak.*

# Test PDFs (Phase 3.2B.1)
test_qt*.pdf
test_po*.pdf
